# GitHub Smart Agent Instructions

- This repository is the public GitHub Pages surface for Han Ryul. Keep public content safe and minimal.
- Do not commit private automation source, credentials, `.env` files, service-account keys, or token helpers here.
- The served site is expected at `https://hanryul32.github.io/promo/life/`; validate the live page and the `gh-pages` branch before changing deployment settings.
- If health checks fail, open or update a `github-smart-agent` issue with the failing log and a concise repair plan.
- Prefer small, reversible changes. Preserve Traditional Chinese public copy unless a task explicitly asks otherwise.